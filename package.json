{
	"scripts": {
		"start": "node index.js"
		, "dev": "cross-env NODE_ENV=development node index.js"

		, "deploy": "git push --force heroku master"
	}
	, "dependencies": {
		"nightmare": "2.x"
	}
	, "devDependencies": {
		"cross-env": "5.x"
	}
	, "engines": {
		"node": "9.x"
	}
}